# RentDrive — Contexto integral del proyecto

> Documento maestro de contexto. Se carga en cada sesión vía `CLAUDE.md`.
> Última actualización del contenido: **2026-07-09**. **Si cambias arquitectura, schema, auth o despliegue, actualiza este archivo.**

## 0. Estado actual y pendientes (al 2026-07-09)

Marca/objetivo del dueño (Victor): ser **el #1 de alquiler de carros en Medellín**. Comunicación en **español**; suele delegar decisiones ("hazlo", "sí", "continúa") y valora que las cosas **funcionen** y se le expliquen los pasos manuales.

**Construido en las últimas sesiones (cada uno tiene su sección detallada abajo):**
- **Campos de matrícula + exención real de pico y placa + retiro del todo riesgo** (rama `feat/campos-vehiculo`, sep-2026 — *pendiente de merge*). **Columnas nuevas en `vehiculos`** (ALTER idempotente en `lib/db.ts`, anotadas en `supabase/schema.sql`, aplicadas también a la BD viva): `combustible TEXT DEFAULT ''` (lista cerrada gasolina|diesel|hibrido|electrico|gas o '' = no declarado), `clase_vehiculo TEXT DEFAULT ''` (la "clase" impresa en la matrícula, texto libre recortado a 40; **no** reemplaza a `tipo`, que sigue siendo la categoría comercial de precio/filtros) y `exencion_pico_placa_inscrita INTEGER DEFAULT 0`. El atajo de IA de la tarjeta de propiedad (`lib/vehiculo-ocr.ts`) ahora también transcribe combustible y clase. **Exención de pico y placa de Medellín** (`lib/vehiculo-campos.ts` → `exentoPicoPlaca`, único punto de verdad, consumido por `placaRestringida` en `lib/pico-placa.ts`): los **eléctricos** quedan exentos automáticamente, pero **híbridos y gas natural (GNV)** solo si el propietario **inscribió el trámite ante la Secretaría de Movilidad** — por eso el formulario pide esa confirmación con una casilla y el servidor la deriva (la fuerza a 0 si el combustible no la requiere). Sin la casilla, un híbrido se trata como restringido normal en calendarios, badge del admin y avisos del cron. **Seguro todo riesgo: retirado del flujo** — DrivePass expide la póliza, así que la clave `todo_riesgo` salió del formulario del propietario, del panel del admin, de la verificación con IA y del checklist de progreso (bajan numerador y denominador: un vehículo incompleto muestra hoy un % menor y más honesto). Los vehículos que ya la subieron **conservan** `documentos.todo_riesgo` en la BD: `PUT /api/vehiculos/[id]` re-inyecta explícitamente toda clave de `documentos` que esté en la base y no venga en el body. Relacionado: `documentosConUrlsValidas` (`lib/storage.ts`) pasó a validar **solo las URLs que cambian** en el request (las ya persistidas se dan por buenas, igual que la allow-list de fotos) — si no, un documento legado con URL anterior a Cloudinary (`/uploads/...`) dejaba la sección Documentos imposible de guardar para siempre. Textos legales/de producto actualizados (`app/terminos`, `app/propietarios-info`, `lib/soporte-agente.ts`): el propietario ya no debe aportar seguro todo riesgo propio.
- **Estandarización de fotos de vehículo con IA** (`lib/estandarizar-foto.ts`, 2026-08-29): al publicar un carro, cada foto (`FotoUpload.tsx` → `POST /api/upload` con `tipo=vehiculo`) pasa, DESPUÉS del difuminado de placa/moderación obligatorios (`lib/blur-placas.ts`, que siguen corriendo siempre para toda imagen sin importar este campo), por un paso adicional puramente cosmético: pipeline `sharp` (contraste/brillo/sombras-luces vía `clahe`, nitidez, resize a 1600px) + **remove.bg** (quita el fondo, `type=car`) + composición sobre un fondo de estudio navy corporativo (degradado de marca) con marca de agua del logo DrivePass en la esquina y sombra suave (SVG) — estilo aprobado por Victor tras ver mockups reales (2026-08-29, ver historial de la sesión), reemplazando el gris genérico inicial —, salida WebP subida a Cloudinary igual que cualquier otra foto. Requiere `REMOVE_BG_API_KEY` (opcional: sin ella, o si algún paso falla/da timeout, se sube la foto tal cual quedó del difuminado — nunca rompe la subida). Se salta si la foto quedó marcada `contenidoSospechoso` (no tiene sentido embellecer algo pendiente de revisión). `/api/upload` quedó restringido a roles `propietario`/`admin` (el único uso legítimo hoy es `FotoUpload.tsx` y `OperacionesPanel.tsx`). Rate limit propio por usuario ya implementado (`consumirIntento('estandarizar-foto:<userId>', 30, 1h)` en `app/api/upload/route.ts`); si se supera, se sube la foto sin este paso cosmético (no bloquea la publicación). Además, cuentas con correo sin verificar (`correoNoVerificado`) también se saltan este paso, para no poder multiplicar el gasto creando cuentas nuevas. **Pendiente real:** la cuenta de remove.bg está en plan gratuito (0 créditos) — `size:'full'` devuelve 402, así que hoy solo entrega resolución "preview" baja (~0.25MP) sin importar el tamaño enviado; hace falta que Victor compre créditos/plan pago en remove.bg para fotos en verdadera alta resolución (cambio de código trivial una vez tenga el plan: `size:'auto'`→`'full'`). Nota aparte: Cloudinary (ya integrado como storage) también ofrece su propio add-on de quitado de fondo — se mantuvo remove.bg porque así se decidió, pero es una alternativa a evaluar a futuro si se quiere un solo proveedor.
- **Precio de mercado automático por vehículo** (`lib/precioMercado.ts`). El precio/día **ya no se fija a mano**: se deriva del **valor comercial + categoría** por interpolación regresiva dentro del rango real de cada segmento (Estudio de Mercado jul-2026, 7 rentadoras): los económicos rentan a ~0,45%/día de su valor, los de gama alta a ~0,20%. Funciones: `precioMercadoSugerido(tipo, valorComercial, ajustePct)`, `bandaPrecioValor(valor)` (rango de cordura para marcar en ámbar un precio fuera de mercado) y `segmentoValido(tipo)` (coacciona tipos legado a un segmento). **Taxonomía unificada:** el `tipo` del vehículo pasó de 4 opciones (sedan/suv/compacto/pickup) a las **6 categorías de la calculadora** (`TipoVehiculo` en `lib/rentabilidad.ts`: sedan/coupe/suv/camioneta7/lujo_auto/lujo_camioneta) — una sola clasificación para registro, vitrina, filtro público (`app/page.tsx`) y cálculo; carros legado remapeados (compacto→sedan). **Registro:** al publicar/editar (`app/dashboard/propietario/page.tsx`) el propietario elige **categoría** y **valor comercial** (ambos obligatorios) y ve el **precio estimado en vivo**; el servidor (`POST/PUT /api/vehiculos`) calcula `precio_dia` solo. **Vitrina:** `VehiculoCard` muestra el precio automático y la etiqueta de categoría legible. **Override manual:** el admin puede escribir un precio a mano → se guarda con `precio_manual=1` y deja de recalcularse (volver a auto = `precio_manual:0`); si está fuera de la banda se avisa. **Calculadora del admin** (`components/CalculadoraPanel.tsx`, pestaña **🧮 Calculadora** — permiso `calculadora` en `lib/permisos.ts`, principal+socio): cotiza un carro que aún **no está en el sistema** (categoría + valor comercial → precio sugerido + banda + rentabilidad completa reusando `calcularRentabilidad`). **Integración** con "¿Cuánto puedes ganar con tu carro?" (`app/calculadora-propietarios/`): el `precioDia` se siembra del precio de mercado interpolado (no un fijo por categoría) y muestra "sugerido de mercado" + aviso de banda → cálculo más preciso. **Columnas nuevas en `vehiculos`:** `valor_comercial REAL`, `precio_ajuste_pct REAL`, `precio_manual INTEGER` (ALTER en `lib/db.ts`; aplicadas también a la BD viva). Los 4 carros existentes ya tenían precio a mano → quedaron marcados `precio_manual=1` (mantienen su precio hasta que el equipo cargue su valor comercial y pase a auto).
- **Panel de Control interno del equipo** (página aparte en **`/control`**, se abre en pestaña nueva desde el botón "🎛️ Panel de control interno" del dashboard admin). Es la capa de trabajo interno para los 3 socios + secretaria (el mensajero sigue en su portal `/m/<token>`). Construido **dentro de la misma app** (SQLite + patrón SSE/sondeo existente, **no** un sistema aparte) tras confirmar en el **Paso 0** que la ventana de Operaciones es 100% interna y no toca lo que ve el cliente (disponibilidad = `vehiculos.disponible` + `reservas` + `dias_disponibles`; `operaciones` nunca escribe a tablas del cliente). Identidad visual propia (asfalto+ámbar, Oswald/IBM Plex) replicando el mockup validado. Layout con **gate de auth server-side** (no-admin → `/login`). Campana de **notificaciones en vivo** (sondeo 12s de `notificaciones`) + toasts. **6 módulos (todos construidos):** **Panel de hoy** (stats + tablero de flota en vivo con estado por vehículo —disponible/alquilado/en operación/no disponible— + pendientes del día derivados de reservas/tareas/eventos), **Tareas** (Kanban Por hacer/En proceso/Hecho, drag&drop + flechas táctiles, asignar responsable/rol, "solo socios"), **Calendario** (grilla mensual; entregas/devoluciones **derivadas de reservas** + eventos/vencimientos manuales), **Documentos** (repositorio con permisos por rol, subida a Cloudinary o link, registro de **quién abre** cada doc en la Bitácora), **Tableros** (canvas infinito tipo Miro: pan/zoom con rueda y **pinch táctil**, herramientas nota/texto/rectángulo/círculo/lápiz/link+imagen, colaboradores **por tablero** —no todos ven todos—, persistencia por elemento y refresco colaborativo cada 7s). Gating por nivel reforzado en **backend** (`lib/guard.ts` + `esSocio` en `lib/permisos.ts`): la secretaria no ve tareas/eventos/documentos "solo socios" y sus intentos de crearlos se degradan en el servidor; los tableros solo los ve su creador/invitados (403 si no). APIs en `/api/control/{resumen,tareas,calendario,documentos,equipo,tableros}`; UI en `app/control/` + `components/control/`. Tablas nuevas: `tareas_equipo`, `eventos_calendario`, `documentos_equipo`, `tableros`, `tablero_colaboradores`, `tablero_elementos`. **Operaciones se movió aquí:** el módulo **Operaciones** ahora vive en `/control` (reusa el mismo `OperacionesPanel` y la misma tabla `operaciones`, montado sobre un lienzo oscuro `.op-embed`) y **se quitó la pestaña del dashboard admin** — una sola ubicación, sin doble fuente de verdad. En **Tareas** se eliminó el campo "Rol" (confuso): ahora solo se elige **Responsable** y la etiqueta de color se deduce sola del nivel de esa persona (`rolTag` en el backend).
- **Gastos de la empresa (Contabilidad)**: sub-pestaña **Gastos** para registrar gastos fijos, variables, facturas de servicios y productos. Se puede **subir PDF/foto** (archivo o cámara) y la IA (Claude vision) **extrae** proveedor, NIT, fecha, subtotal, IVA, total y valor abonado; la imagen **queda guardada como soporte** (Cloudinary). Soporta **pago mixto** (un campo por medio de pago), **total vs. abonado/saldo**, **ver/editar**, **anular** (con confirmación → van a "Anulados", no se borran), **descargar PDF** del gasto y **subir comprobante de pago**. Resumen y filtros por defecto desde el **inicio del año** (no solo mes actual). Thumbnails optimizados con transformaciones Cloudinary (carga rápida).
- **Remisiones automáticas**: al confirmarse el pago del cliente se genera una **remisión a nombre del dueño del vehículo** (`lib/contabilidad.ts` → `generarRemision`, tabla `remisiones`). Al pagar la liquidación al propietario se puede **subir el comprobante** y queda en el historial como soporte.
- **Roles dentro del admin + auditoría** (correos independientes → trazabilidad). Sub-nivel `usuarios.admin_nivel`: **principal** (dueño, todo), **socio** (todo menos crear cuentas de equipo, editar config y ver bitácora completa según matriz), **secretaria** (punto de atención: reservas, operaciones, leads y soporte; **sin** contabilidad, usuarios, config ni auditoría). Matriz única en `lib/permisos.ts` (`AREA_NIVELES`, `puede()`), compartida por cliente (oculta pestañas) y servidor (`lib/guard.ts` → `guardArea()` devuelve 403). Toda acción sensible se registra en la tabla **`auditoria`** (`registrarAuditoria`) y se ve en la pestaña **🧾 Bitácora** (`AuditoriaPanel`): quién, qué, correo, área y hora. El **principal** crea cuentas del equipo y cambia niveles desde Usuarios (con auto-protecciones: no puede quitarse su propio principal ni desactivarse a sí mismo). **Permisos por empleado (casillas):** encima del nivel, cada cuenta admin puede tener **excepciones** en `usuarios.permisos_extra` (JSON `{ area: boolean }`); regla efectiva = si el área está en las excepciones manda la excepción, si no manda `AREA_NIVELES[area]` — una cuenta sin excepciones se comporta idéntico a antes. Se editan desde Admin → Usuarios → **🔐 Secciones** (casilla por sección, etiqueta `nivel` = heredado / `excepción` = a la medida, botón “Volver todo al nivel”); solo el **principal** las reparte, **nadie puede editar las propias** (403 del servidor) y **`usuarios_gestion` NO es asignable por casilla** (raíz de confianza: quien la tuviera podría auto-asignarse todo). Lista blanca de áreas + auditoría (`cambiar_permisos`) en cada cambio. El contenido “solo socios” sigue atado al nivel (`esSocio`), no a las casillas.
- **Soporte iniciado por el admin**: el admin ahora **puede iniciar una conversación** con un propietario o usuario ("＋ Nueva conversación" en SoportePanel → busca contacto en `/api/soporte/contactos`, escribe y envía por `/api/soporte`). Antes solo podía responder conversaciones existentes.
- **Verificador de documentos con IA** (Claude vision): vehículo + arrendador + arrendatario; modo **híbrido** (auto-aprueba alta confianza, resto a revisión humana). Cruce **foto-contra-foto** de tarjeta de propiedad vs. cédula del propietario.
- **Visualización de documentos en admin**: cédula del propietario (Usuarios → perfil) y **cédula + licencia del cliente** (tarjeta de reserva → "Documentos del cliente").
- **Operaciones / logística**: al confirmar una reserva se crea un servicio con checklist (lavar, tanquear, entregar, recibir, inspección); pestaña **Operaciones** (asignar mensajero, checklist, notas, reenvío).
- **Acceso del mensajero por formal enlace/token** (`/m/<token>`, sin contraseña, móvil): ve sus servicios, marca checklist, sube fotos y corre la inspección.
- **Inspección de daños con IA**: compara fotos de **salida vs. entrada** y detecta daños nuevos (rayones, abolladuras, vidrios, etc.).
- **WhatsApp** (helper `lib/whatsapp.ts`): **apagado por defecto** (`WHATSAPP_ENABLED`); usa el bridge personal de hermes en `localhost:3000`.
- **Pico y placa**: editor de rotación manual (pestaña **Configuración**), resaltado de carros restringidos hoy en la lista, y avisos a propietario+cliente (cron 6am vía `CRON_SECRET` + `scripts/pico-placa-cron.sh`).
- **Fix**: `POST /api/vehiculos` no guardaba `placa` (bug); ya guarda. Campo de placa editable en cada carro del dashboard del propietario.

**Pendientes del lado del usuario (manual):**
1. Poner `ANTHROPIC_API_KEY` en `rentdrive/.env.local` → activa verificador e inspección IA (sin ella, esos endpoints dan 503 amable). `.env.local` ya existe con la línea vacía.
2. **Llenar las placas** de los 4 carros existentes (Corolla, CX-5, Spark, Tucson) → requisito del pico y placa (sin placa no hay dígito que comparar).
3. Activar el **cron de pico y placa** (comando en la entrega) y/o `WHATSAPP_ENABLED=true` para envíos reales.
4. Para que el **enlace del mensajero*j abra desde el celular: misma WiFi + `APP_URL` con la IP LAN de la Mac (hoy `192.168.1.34:3100`) o un dominio/túnel en producción.
5. **Crear las cuentas del equipo** desde Admin → **Usuarios** (solo el dueño/**principal**): la **secretaria** (nivel `secretaria`) y los **2 socios** (nivel `socio`) con sus correos independientes. La empresa son **3 socios (60/20/20)** + secretaria en punto de atención + mensajero. Cada cuenta con su correo → la **Bitácora** registra quién hizo cada cambio. (El reparto 60/20/20 es societario/contable; el sistema no lo automatiza hoy.)

**Tema en pausa (asesoría dada, sin construir):** integración de **comparendos/fotomultas**. NO hay API pública del RUNT/SIMIT (captcha, solo entidades autorizadas). Quedó la recomendación: chequeo manual semanal del SIMIT por placa, o contratar una API comercial y enchufarla a un conector + cron. Las fotomultas tardan **15–30 días** en aparecer. Cláusula de contrato: el **arrendatario** asume infracciones durante el alquiler.

**Pagos con Wompi (2026-09-17, reconciliado con el panel nuevo el 2026-09-22) — EN PRODUCCIÓN:**
- Se descartó Bold; la pasarela elegida es **Wompi**. Integración directa por API (sin el widget hospedado de Wompi), así que **no se usa firma de integridad de Wompi en el checkout** (esa firma es solo para el Web Checkout) — sí se usa una firma de integridad propia (SHA256 con `WOMPI_INTEGRITY_SECRET`) al crear cada transacción por API, como exige Wompi incluso fuera del widget.
- `lib/pagos.ts`: cliente de Wompi — `obtenerTokensAceptacion`, `crearFuentePago`, `crearTransaccion`, `consultarTransaccion`, `esperarResultadoTransaccion`, `anularTransaccion`, `verificarFirmaEvento`.
- El alquiler se cobra **de una vez al reservar** (no se espera la aprobación operativa, pero la reserva sí queda `estado='pendiente'` hasta que el equipo la apruebe): `app/pago/page.tsx` tokeniza la tarjeta directo en el navegador contra Wompi (el número y CVV nunca tocan nuestro backend) y `app/api/reservas/route.ts` (POST) — ya integrado con TODA la lógica de `lib/reserva-core.ts` (gates de correo verificado/perfil completo, créditos de referido, cotización, etc.) — crea la fuente de pago + cobra la transacción ANTES de llamar `insertarReserva`. Si el banco rechaza, no se crea la reserva.
- Tablas en `lib/db.ts`: `fuentes_pago` (tarjeta tokenizada por usuario, reutilizable) y `cargos_extra` (multas/daños, cobro 100% manual por el admin, protegido por `adminTieneArea(db, userId, 'reservas')`). Columna `reservas.wompi_transaccion_id` (se fija con un UPDATE aparte tras `insertarReserva`, porque esa función es compartida con la vía de mostrador y no conoce Wompi).
- `app/api/reservas/[id]/cargos/route.ts`: admin (con permiso de sección "reservas") registra un cargo (multa/daño/otro) con monto y descripción, y se cobra de inmediato contra la fuente de pago guardada del cliente — no hay ninguna automatización con SIMIT.
- `app/api/webhooks/wompi/route.ts`: recibe `transaction.updated`, valida el checksum SHA256 con `WOMPI_EVENTS_SECRET`, y es la única vía para confirmar pagos que quedan en `PENDING` tras el intento síncrono. En Wompi (modo Producción → Desarrollo → Programadores) la URL de eventos apunta a `https://www.drivepasscol.com/api/webhooks/wompi`.
- Al rechazar una reserva ya cobrada (`app/api/reservas/[id]/route.ts`), el backend intenta anular la transacción en Wompi (`POST /transactions/:id/void`); si falla, `pago_estado` no cambia y el admin dashboard avisa que debe reembolsar manualmente. El PUT genérico de reservas **ya no acepta `pago_estado` desde el cliente** salvo del admin (para las reservas de mostrador, pago manual efectivo/transferencia/datafono) — el cobro online real lo fija el cobro con Wompi, el webhook o esta anulación, nunca el body de un `usuario`/`propietario`.
- **UI del admin**: vive en `components/panel/ReservasSeccion.tsx` (compartido entre `/dashboard/admin` y `/panel`), NO en `app/dashboard/admin/page.tsx` (ese archivo quedó reducido a un shell que monta las secciones del panel nuevo). Ahí está el badge 💳 de `pago_estado`, el botón "Cargos extra" y su modal, y `aprobarReserva`/`rechazarReserva` ya NO mandan `pago_estado` desde el cliente (lo fija el servidor con el cobro/anulación real).
- Variables en Railway (producción, puestas el 2026-09-22): `NEXT_PUBLIC_WOMPI_PUBLIC_KEY`, `WOMPI_PRIVATE_KEY`, `WOMPI_EVENTS_SECRET`, `WOMPI_INTEGRITY_SECRET`, `WOMPI_BASE_URL=https://production.wompi.co/v1`. En local (`.env.local`) se dejaron las de **sandbox** a propósito, para poder seguir probando sin cobrar con tarjetas reales.
- Probado end-to-end (sandbox) antes de pasar a producción: cobro al reservar, cobro de cargo extra, webhook (firma válida/ inválida), y anulación al rechazar — los 4 verificados contra la API real de Wompi, incluyendo una corrida completa por `POST /api/reservas` con todos los gates nuevos activos.

**Gotchas operativos (IMPORTANTE para no repetir errores):**
- Dev server en **puerto 3100** (`next dev -p 3100`); el 3000 es el bridge de WhatsApp de hermes. **No correr `npm run build` con el dev activo** (corrompe `.next`).
- Validar con `npx tsc --noEmit` y `eslint`. Hay errores de eslint **preexistentes en todo el proyecto** (`react-hooks/set-state-in-effect`, `immutability` en los `useEffect` de carga, `no-img-element`): NO son míos, no bloquean dev/build. tsc debe quedar en 0.
- Las **migraciones `ALTER TABLE` no se aplican solas** a `rentdrive.db` porque la conexión es un singleton; tras editar `lib/db.ts` hay que aplicar el ALTER también a la BD viva con un script node puntual (better-sqlite3) o reiniciar el server.
- A veces el sandbox no alcanza `localhost:3100` por curl; verificar por `tsc`/lint/disco y por el log del dev. Cuando sí responde: 403/404 = la ruta compiló y el gating funciona; 500 = error real.
- Modelo de la API de Claude: `claude-opus-4-8`, visión por base64, salida estructurada `output_config.format.json_schema` + `effort:'high'` + `thinking:{type:'adaptive'}`.

## 1. Qué es

**RentDrive** (marca también referida como *DrivePass*) es una plataforma web de **alquiler de carros entre particulares** en Colombia (Medellín por defecto). Conecta **propietarios** que publican vehículos con **usuarios** que los reservan, con un **admin** que supervisa. Incluye chat entre las partes, gestión de documentos/licencias, reservas con fotos antes/después, contratos firmados y notificaciones.

## 2. Stack y estructura

- **Framework**: Next.js **16** (App Router) + React **19** + TypeScript.
- **Estilos**: Tailwind CSS 4 (`@tailwindcss/postcss`).
- **DB (realidad actual del código)**: **SQLite** vía `better-sqlite3`, archivo `rentdrive.db` en la raíz de `rentdrive/`. El schema se crea/migra en `lib/db.ts` (`CREATE TABLE IF NOT EXISTS` + `ALTER TABLE` defensivos).
- **DB (destino de despliegue)**: **Postgres en Supabase**. El schema equivalente vive en `supabase/schema.sql` y datos en `supabase/seed.sql`. ⚠️ **Hay divergencia**: el código corre en SQLite; Supabase es el plan de producción. Mantén ambos en paridad cuando cambies el schema.
- **Auth**: JWT propio (`jsonwebtoken`) + `bcryptjs`. Token en cookie `httpOnly`. **No** usa Supabase Auth.
- **Storage (realidad actual)**: filesystem local `public/uploads/` (`lib/storage.ts`). En producción el README plantea Supabase Storage (aún no implementado en el código).
- **PWA**: `public/manifest.json` + `public/sw.js` + `components/SwRegistrar.tsx`.
- **i18n**: `contexts/LanguageContext.tsx` + `lib/i18n.ts`.
- **Exportación**: `lib/export.ts` con `jspdf` / `xlsx` (PDF/Excel de reservas).
- **Reglas locales**: `lib/picoYPlaca.ts` (pico y placa Colombia).
- **Lugares de recogida/entrega**: `lib/lugares.ts` (catálogo de munici*
### Carpetas clave (`rentdrive/`)
```
app/                 App Router (páginas + API)
  api/               Endpoints backend
  (auth)/            login, registro
  dashboard/         admin | propietario | usuario
  vehiculos/[id]/    detalle de vehículo
  chat/, historial/, pago/, ...
components/          UI compartida (Navbar, BottomNav, cards, uploads, calendarios)
contexts/            SessionContext, LanguageContext
lib/                 db, auth, storage, export, i18n, picoYPlaca, sound
supabase/            schema.sql, seed.sql, _export_data.py (paridad Postgres)
public/uploads/      archivos subidos (local)
rentdrive.db         base SQLite local
```
