export async function exportarExcel(reservas: unknown[], rol: string, filtrosLabel: string) { void reservas; void rol; void filtrosLabel; }
export async function exportarPDF(reservas: unknown[], rol: string, filtrosLabel: string, stats: unknown) { void reservas; void rol; void filtrosLabel; void stats; }